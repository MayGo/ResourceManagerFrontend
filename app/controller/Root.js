/**
 * The main application controller. This is a good place to handle things like routes.
 */
Ext.define('ResourceManager.controller.Root', {
    extend: 'Ext.app.Controller',
    requires: [
        'ResourceManager.view.main.Main'
    ],
});
